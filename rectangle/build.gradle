apply plugin: 'java'

sourceCompatibility = 1.7
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':interview-common')
    
    compile 'com.google.collections:google-collections:1.0-rc2'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}